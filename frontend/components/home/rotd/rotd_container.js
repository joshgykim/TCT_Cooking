import { connect } from "react-redux";
import ROTD from "./rotd";

const mapStateToProps = state => ({
})

const mapDispatchToProps = dispatch => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(ROTD);